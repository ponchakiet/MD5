version: '3.8'

services:
  # ------------------------------------------------
  # Service 1: Database PostgreSQL theo yêu cầu
  # ------------------------------------------------
  core-db:
    image: postgres:15-alpine
    container_name: core-db
    environment:
      POSTGRES_DB: banking_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: kimquan1
    ports:
      - "5432:5432"
    restart: unless-stopped

  # ------------------------------------------------
  # Service 2: Ứng dụng Spring Boot
  # ------------------------------------------------
  banking-app:
    build: .
    container_name: banking-app
    environment:
      # Truyền biến môi trường để trỏ URL kết nối database tới service core-db
      SPRING_DATASOURCE_URL: jdbc:postgresql://core-db:5432/bank_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: kimquan1
    ports:
      - "8080:8080"
    depends_on:
      - core-db